STATUS
- Add extra status info including the ports redirected to HERE from the other peers. Since the redirected ports are not defined in the local config, we need to find a way to get and report them. Maybe we should construct an in-app state export method somehow. -> monitor port? or periodical file dump? etc.
- pid detection is not working well in windows. It say the pidfile exists but the app is dead while it is not.

LOGGING
- Improve logging. Make it more human readable rather than being cryptic and semi-structured.

CLIP SYNC
- ClipSync support -> LAN mode vs WAN mode. flaggable, configurable.

PERFORMANCE / UX
- Support mux / multiplex connection type to allow creating a ConnectionMaster for a specific connection path.
- Allow granular control of connections without restarting the whole app. -> is this really necessary? I think NO.
